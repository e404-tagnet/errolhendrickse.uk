{
  "scripts": {
    "build": "rm -rf public/* && mkdir -p public/assets public/artworks && cp index.html about.html collection.html prints-sketches.html placeholder.html README.md public/ && cp -r assets/* public/assets/ && cp -r artworks/* public/artworks/",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "wrangler": "^4.120.0"
  }
}
